/**
 * @file sodchan_crypto.c
 * @brief libsodium crypto helpers (PR-2).
 *
 * Scaffold placeholder so the translation unit is non-empty under -Wpedantic.
 * Real keygen / fingerprint / KDF land in PR-2.
 */

#include "sodchan_internal.h"

/* Satisfies -Wpedantic until PR-2 provides symbols. */
int sodchan_crypto_scaffold_ok(void)
{
    return 1;
}
